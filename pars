SELECT 
    tm.*,
    ts.*,
    th.*
FROM 
    THRESHOLD_MASTER tm
JOIN 
    THRESHOLD_SET ts 
    ON tm.TSHLD_SET_TEMPLATE_ID = ts.TSHLD_SET_TEMPLATE_ID
JOIN 
    THRESHOLDS th 
    ON ts.TSHLD_SET_ID = th.TSHLD_SET_ID;
